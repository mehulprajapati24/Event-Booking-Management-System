<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta content="" name="description">
    <meta content="" name="author">
    <meta content="" name="keywords">
    <title>The Event</title>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css"/>

    <link rel="stylesheet" href="/css/login.css">
    </head>
    <body>
        <div class="bg_image"></div>
        <div class="center">
            <h1>Changed Password</h1>

            <form action="/theevent/coordinator/passwordchanged" method="post" id="changedPasswordForm">

                <% if(locals.email){ %>
                <input id="hiddenEmail" name="email_id" value="<%= email %>" hidden></input>
                <% } %>

                <div class="txt_field row_field left" style="display: block; margin: 40px auto;">
                    <input type="password" name="password" id="password-input" required>
                    <span></span>
                    <Label>New Password</Label>
                    <i class="fa-solid fa-eye-slash eye" id="togglePassword"></i>
                    <h5 id="error" class="pass_error">Password must contain at least 4 characters</h5>
                </div>

                <div class="txt_field row_field left" style="display: block; margin: 40px auto;">
                    <input type="password" name="confirm_password" id="password-input2" required>
                    <span></span>
                    <Label>Confirm Password</Label>
                    <i class="fa-solid fa-eye-slash eye" id="togglePassword2"></i>
                    <h5 id="error" class="pass_error">Passwords doesn't match</h5>
                </div>

                <input type="submit" value="Reset Password" style="margin-bottom: 30px; width: 45%; position: relative; left: 50%; transform: translateX(-50%);">
            </form>
        </div>

        <script src="/js/newpassword.js">
        </script>

    </body>
    </html>