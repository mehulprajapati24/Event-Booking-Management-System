<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta content="" name="description">
    <meta content="" name="author">
    <meta content="" name="keywords">
    <title>The Event</title>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css"/>

    <link rel="stylesheet" href="/css/login.css">
    </head>
    <body>
        <div class="bg_image"></div>
        <div class="center">
            <h1>Registration</h1>

            <form action="/theevent/user/otp" method="post" id="registrationForm">
                <div class="year">
                    <select class="year" id="year" name="year">
                        <option value="Select Year" disabled selected>Select Year</option>
                        <option value="1">1</option>
                        <option value="2">2</option>
                        <option value="3">3</option>
                        <option value="4">4</option>
                    </select>
                    <h5 id="error" class="year_error" style="top: 52px;">Please select year!</h5>
                </div>

                <div class="department">
                    <select class="department" id="department" name="department">
                        <option value="Select Department" disabled selected>Select Department</option>
                        <% for(i=0; i<departments.length; i++) { %>
                            <option value="<%= departments[i]%>"><%= departments[i] %></option>
                        <% } %>
                    </select>
                    <h5 id="error" class="department_error" style="top: 52px;">Please select department!</h5>
                </div>

                <div class="txt_field enrollment">
                    <input type="number" name="enrollment" id="" required>
                    <span></span>
                    <Label>Enrollment Number</Label>
                </div>

                <div class="txt_field row_field right">
                    <input type="password" name="password" id="password-input" required>
                    <span></span>
                    <Label>Create Password</Label>
                    <i class="fa-solid fa-eye-slash eye" id="togglePassword"></i>
                    <h5 id="error" class="pass_error">Password must contain at least 4 characters</h5>
                </div>

                <div class="txt_field row_field left">
                    <input type="password" name="confirm_password" id="password-input2" required>
                    <span></span>
                    <Label>Confirm Password</Label>
                    <i class="fa-solid fa-eye-slash eye" id="togglePassword2"></i>
                    <h5 id="error" class="pass_error">Passwords doesn't match</h5>
                </div>

                <div class="pass pass_reg">Forgot Password?</div>
                <input type="submit" value="Send OTP">
                <div class="signup_link">
                    Already have an account? <a href="/theevent/user/login">Login</a>
                </div>
            </form>
        </div>

        <script src="/js/registration.js"></script>

    </body>
    </html>