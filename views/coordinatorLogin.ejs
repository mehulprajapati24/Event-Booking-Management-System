<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta content="" name="description">
    <meta content="" name="author">
    <meta content="" name="keywords">
    <title>The Event</title>

    <link rel="icon" type="image/png" href="/img/favicon.png">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css"/>

    <link rel="stylesheet" href="/css/login.css">

</head>
<body>
    <div class="bg_image"></div>
    <div class="center">
        <h1>COORDINATOR LOGIN</h1>
        <form action="/theevent/coordinator/login" method="post" name="loginForm">
            <div class="txt_field bottom">
                <% if(locals.existEmailId){ %>
                    <input type="email" name="email" id="emaillogin" value="<%= existEmailId %>" required>
                <% } else{ %>
                <input type="email" name="email" id="emaillogin" required>
                <% } %>
                <span></span>
                <Label>Email ID</Label>
                <% if(locals.emailId){ %>
                <h5 id="emailError" class="emailerror" style="display: block;"><%= emailId %></h5>
                <% } %>
            </div>
            <div class="txt_field bottom">
                <% if(locals.existPassword){ %>
                    <input type="password" name="password" id="emailpassword" value="<%= existPassword %>" required>
                <% } else{ %>
                <input type="password" name="password" id="emailpassword" required>
                <% } %>
                <span></span>
                <i class="fa-solid fa-eye-slash eyePassword" id="togglePassword"></i>
                <Label>Password</Label>
        
                <% if(locals.password){ %>
                <h5 id="passwordError" class="passworderror" style="display: block;"><%= password %></h5>
                <% } %>
            </div>
            <div class="pass" onclick="window.location.href='/theevent/coordinator/forgot-password'">Forgot Password?</div>
            <input type="submit" value="Login" style="margin-bottom: 40px;">
        </form>
    </div>    

    <script src="/js/login.js"></script>
</body>
</html>